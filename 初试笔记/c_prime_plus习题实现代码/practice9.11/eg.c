#include "eg.h"

int main(void)
{
	/* //eg1
	double a, b;
	printf("Please input two numbers:\n");
	while((scanf_s("%lf %lf", &a, &b))==2)
		printf("The min number is %.2f.", min(a, b));
	*/

	/*
	double a, b;
	getnumber(&a,&b);
	eg4(a, b);
	*/

	eg7();

	return 0;
}
